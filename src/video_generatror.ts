import { ImageGenerator } from "./image_generator.js";
import { InputDefinition, InputDefinitionHelper } from "./input_definition.js";


export class VideoGenerator
{
    generateVideo(imageGenerator : ImageGenerator){
        const inputDefinitions = InputDefinitionHelper.loadInputDefinitions("inputDefinition.json");
        //fs.readFile()
    }

    
}